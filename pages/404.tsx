const NotFoundPage = (props: { [key: string]: string }) =>
  <div className="contents error">
    <h1>404: PAGE NOT FOUND</h1>
    <p>The provided web address is wrong or outdated.</p>
  </div>

export default NotFoundPage
